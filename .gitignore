# ============ Secrets (NEVER commit) ============
.secrets/
*.env
.env
.env.*
!.env.example

# ============ Sensitive runtime data ============
api_cache/
api_usage.json
*.pem
*.key

# ============ Internal AI company operations (not for public repo) ============
# 整个 company/ 目录都是 AI 公司内部运营数据：会议记录、报告、营销物料、
# 旧项目（seo-content-generator/devto-launch/upwork-launch）、PDF 产品等。
# 对外开源仓库只展示产品级代码（api_server / autocorp_agents / autocorp_paypal_cli）。
company/

# ============ IDE working files (specs/sessions/checklists) ============
.trae/

# ============ Browser profile (contains MetaMask extension data) ============
chrome-profile/
PlaywrightProfile/
playwright-profile/
.browser-profile/

# ============ Scripts containing sensitive strings (for replacement) ============
scripts/sanitize_for_github.py

# ============ Python ============
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
pip-log.txt
pip-delete-this-directory.txt

# ============ Virtual envs ============
venv/
env/
ENV/
.venv/
.env/

# ============ IDE ============
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# ============ OS ============
Thumbs.db
desktop.ini

# ============ Logs ============
*.log
logs/

# ============ Cache ============
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.tox/

# ============ Node (for any future frontend) ============
node_modules/

# ============ Build artifacts ============
*.pdf
